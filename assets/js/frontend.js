/**
 * MP Directory Frontend JavaScript
 *
 * @package MP_Directory
 */

(function ($) {
  "use strict";

  $(document).ready(function () {
    // Smooth scroll for back links
    $(".mp-back-link").on("click", function (e) {
      // Allow default behavior but add smooth scroll if needed
    });

    // Add any frontend interactivity here
    // For example: AJAX filtering, infinite scroll, etc.
  });
})(jQuery);
